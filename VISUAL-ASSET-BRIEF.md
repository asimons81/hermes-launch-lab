# Launch Lab visual asset brief

Status: approval required before image generation or screenshot capture.

All assets must use real, sanitized source material; contain no client data, credentials, private repository details, or invented telemetry; ship as AVIF/WebP with explicit dimensions; and include a mobile crop. Illustrative material must be labeled as such.

| Page | Asset | Purpose and required content | Desktop / mobile | Authenticity label |
| --- | --- | --- | --- | --- |
| Home | Annotated workflow receipt | Show goal → access boundary → execution → verification → handoff beside the existing simulated terminal. Use a sanitized real workflow receipt. | 1440×900 / 720×900 | `SANITIZED EXAMPLE — NOT LIVE CLIENT DATA` |
| About | Project receipt set | Replace the three generic thumbnails with consistent captures from Hermes Vault, NexusOS, and Hardproof showing one source-backed capability each. | 1200×750 / 720×720 | `PUBLIC PROJECT RECEIPT` |
| What I Build | System architecture | Explain machine → Hermes → permission/secret boundary → skill → verified output. Prefer CSS/HTML for labels and accessibility; use imagery only for authentic interface captures. | Responsive HTML diagram | `SYSTEM MODEL` |
| What I Build | Example workflow | One sanitized real workflow with inputs, operations, and verification evidence. | 1440×900 / 720×900 | `SANITIZED EXAMPLE` |
| Pricing | Outcome comparison | Strategy → written plan; Launch → one tested workflow; Custom → approved written scope. Keep offers above the fold. | Responsive HTML diagram | None needed |
| Sessions | Privacy/control flow | Google Meet → one-time attended access → client enters secrets → verify → disconnect. Implemented as accessible HTML/CSS. | Responsive HTML diagram | None needed |
| Docs | Command receipts | Add only where a command or expected result benefits from a real sanitized terminal capture. | 1200×675 / 720×720 | `EXAMPLE OUTPUT` |

Founder photography stays real and lightly processed. FAQ, booking, intake, legal, portal, contact, and status remain interface-led without decorative hero art.
